AuthenticationMode=USER_AND_APPLICATION;UUID=YOUR_UUID;APPNAME=YOUR_APPNAME

String authOptions = "AuthenticationMode=USER_AND_APPLICATION;UUID=" + UUID + ";APPNAME=" + APP_NAME;
            SessionOptions sessionOptions = new SessionOptions();
            sessionOptions.setServerHost(SERVER_HOST);
            sessionOptions.setServerPort(SERVER_PORT);
            sessionOptions.setAuthenticationOptions(authOptions);

String sql = "SELECT email FROM user WHERE nid = ?";
        try {
            return jdbcTemplate.queryForObject(sql, new Object[]{nid}, String.class);
        } catch (EmptyResultDataAccessException e) {
            // No matching record found — return null
            return null;
        }

list.sort(
    Comparator.comparing(
        item -> item.getArchivedDate() != null ? item.getArchivedDate() : item.getLastModifiedDate(),
        Comparator.nullsLast(Comparator.reverseOrder())
    )
);

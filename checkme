@GetMapping("/{date}")
    public ResponseEntity<String> compare(@PathVariable String date) {
        // date passed as yyyy-MM-dd
        String result = compareService.compareFolderWithTable(date);
        return ResponseEntity.ok(result);
    }
